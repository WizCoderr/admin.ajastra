import { setToken, getToken, removeToken, setRole, getRole } from './auth';
import endpoints from './baseApi';
import Api from './Axios';

export { setToken, getToken, removeToken, endpoints, Api, setRole, getRole };
